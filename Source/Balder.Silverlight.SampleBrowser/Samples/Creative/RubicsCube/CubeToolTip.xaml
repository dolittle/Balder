<UserControl x:Class="Balder.Silverlight.SampleBrowser.Samples.Creative.RubicsCube.CubeToolTip"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding X}" Grid.Column="0" Grid.Row="0"/>
            <TextBlock Text="{Binding Y}" Grid.Column="1" Grid.Row="0"/>
            <TextBlock Text="{Binding Z}" Grid.Column="2" Grid.Row="0"/>
        </Grid>

    </Grid>
</UserControl>
